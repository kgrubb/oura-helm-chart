CronJob {{ include "oura.fullname" . }} runs {{ .Values.schedule }} ({{ .Values.timeZone }}).
Postgres Secret: {{ include "oura.postgresSecretName" . }}
Auth Secret: {{ include "oura.authSecretName" . }}
{{- if eq (include "oura.bootstrapEnabled" .) "true" }}
Admin Secret: {{ include "oura.adminSecretName" . }} (bootstrap hook)
{{- end }}
{{- if eq (include "oura.dashboardEnabled" .) "true" }}
Dashboard datasource UID: {{ .Values.dashboard.datasourceUid }}
{{- end }}
{{- if eq .Values.auth.mode "oauth" }}
OAuth token PVC: {{ include "oura.tokenPvcName" . }}
{{- end }}
